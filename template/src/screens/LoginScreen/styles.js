import {createStylesheet} from '../../utils/styleUtils';

const styles = createStylesheet({
  input: {
    fontSize: 16,
    height: 48,
    padding: 8,
    borderRadius: 8,
    borderWidth: 1,
    borderColor: '#EEE',
    marginBottom: 16,
  },
});

export default styles;
