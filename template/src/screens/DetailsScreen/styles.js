import {createStylesheet} from '../../utils/styleUtils';

const styles = createStylesheet({
  title: {
    fontSize: 24,
    fontWeight: 'bold',
    marginBottom: 24,
  },
  body: {
    fontSize: 16,
    marginBottom: 24,
  },
});

export default styles;
